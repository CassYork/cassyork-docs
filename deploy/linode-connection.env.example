# Copy to deploy/linode-connection.env (gitignored) so Makefile / push script know your VM.
# Get IP from: linode-cli linodes list --json | jq ...
#
LINODE_HOST=203.0.113.10

# Optional overrides:
# LINODE_SSH_USER=root
# LINODE_REMOTE_DIR=/opt/cassyork-docs
