# Copy to deploy/.env.linode on the server and fill in secrets.
# docker compose --env-file .env.linode -f docker-compose.linode.yml up -d

# --- Postgres (shared by Temporal metadata + app DB cassyork) ---
POSTGRES_USER=temporal
POSTGRES_PASSWORD=change-me-strong

# DATABASE_URL is constructed in docker-compose.linode.yml from POSTGRES_USER + POSTGRES_PASSWORD.

# --- Temporal ---
TEMPORAL_ADDRESS=temporal:7233
TEMPORAL_NAMESPACE=default

# --- Object storage ---
# Recommended on Linode: Akamai Object Storage (S3). Generate keys and merge env with:
#   LINODE_OS_BUCKET=my-bucket LINODE_OS_REGION=us-east ./deploy/scripts/configure-linode-object-storage.sh --apply --create-key
# Linode endpoint/path-style defaults applied by that script (HTTPS + virtual-hosted style).
#
# Bundled MinIO (same Docker host, optional profile — requires OBJECT_STORAGE_* pointing at minio:9000):
#   docker compose --profile minio --env-file deploy/.env.linode -f deploy/docker-compose.linode.yml up -d --build
OBJECT_STORAGE_SCHEME=s3
OBJECT_STORAGE_ENDPOINT=https://us-east-1.linodeobjects.com
OBJECT_STORAGE_REGION=us-east-1
OBJECT_STORAGE_BUCKET=cassyork-documents
OBJECT_STORAGE_ACCESS_KEY_ID=
OBJECT_STORAGE_SECRET_ACCESS_KEY=
OBJECT_STORAGE_USE_PATH_STYLE=false

# --- Optional telemetry ---
# OTEL_EXPORTER_OTLP_ENDPOINT=otel-collector:4317

# --- Admin UI ---
LISTEN_ADDR=:8095
# ADMIN_UI_LIST_LIMIT=200

# Optional: link from Settings page (use your VM IP or DNS)
# TEMPORAL_UI_URL=https://temporal.yourdomain.com
# WEBHOOK_API_PUBLIC_URL=http://YOUR_LINODE_IP:8095
